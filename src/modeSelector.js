Implement a `MODES` model (`id`, `name`, `summary`, `teamSizes`, `perks`, `accent`) and `initModeSelector({ rootId, heroLabelId, hiddenInputId })`. Render selectable mode buttons, support click + arrow key navigation, and maintain `aria-pressed`. On selection: set `document.documentElement.dataset.mode`, update hero label text, update a hidden form input if present, persist to `localStorage` (`fg1.mode`), and sync `?mode=` query param without page reload. Export via `module.exports` and attach browser API to `window.modeSelector`.
