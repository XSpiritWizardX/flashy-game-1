Add a MODES array (or move it to src/modes.js) with id, name, summary, teamSizes, perks, and accent. Export initModeSelector({rootId, heroLabelId, hiddenInputId}) that renders buttons into the root, applies aria-pressed, and wires click/arrow-key handlers. On selection, set document.documentElement.dataset.mode, update the hero label, set the hidden input value if present, write localStorage.setItem('fg1.mode', id), and update the mode query param without reload.
